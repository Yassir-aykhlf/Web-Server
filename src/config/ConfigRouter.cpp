#include "ConfigRouter.hpp"

// here i will define the routing logic for the configuration
// this will include matching the configuration directives to their respective

// an empthy file is valid 
// also if the file contains only comments and whitespace it is also valid
// if there is something missing there should be a default value routed
// for example if there is no listen directive, it should default to 0.0.0:80

class ConfigRouter
{
};