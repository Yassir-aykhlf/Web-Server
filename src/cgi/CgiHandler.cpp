/*
Manages the execution of external scripts (Python/Bash).
It handles fork(), execve(), and pipe() creation.
It translates HTTP request data into Environment Variables required by the CGI standard.
 */