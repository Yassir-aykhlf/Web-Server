/*
A Finite State Machine that incrementally parses raw bytes from the network into a structured HttpRequest object.
It handles request lines, headers, fixed-length bodies, and Chunked Transfer Encoding.
*/